# docs/archive — obsolete but historically valuable

Documents that no longer describe the product but are kept as the historical record (the hackathon
era). Each carries an **ARCHIVED** banner with its supersession pointer. Not maintained — do not cite
them for current behaviour: current docs live one level up in `docs/`, the decision record in
`docs/adr/`, and the roadmap in `docs/plans/` + the plan tracker issue (#113). `docs/plans/` and
`docs/handoffs/` are the historical record *by design* (numbered series) and are never moved here.
